# Segredos / config local
.devcontainer/.env

# .env da APLICAÇÃO na raiz (segredos: Postgres, Telegram, proxy)
/.env

# Python
__pycache__/
*.py[cod]
.venv/
uv.lock.tmp

# undetected-chromedriver (binário baixado em runtime)
*chromedriver*
